import LabelManager from '../../../../javascript/managers/label-manager/LabelManager.js';

const parentId = 'pageWrapper'
const id = 'label_fullname'

const labelFullName = new LabelManager(parentId, id)

export default labelFullName